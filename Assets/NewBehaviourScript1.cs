using System.Collections;
using UnityEngine;

namespace Assets
{
    public class WordSong : MonoBehaviour
    {
        // 歌詞を表示させるためのカウンター
        private float time_count = 0.0f;

        // 歌詞を表示させる時間
        private float[] view_time_count = null;

        // 出現させる歌詞
        private string[] word_song = null;
        
        // Use this for initialization
        void Start()
        {
            // 選択した音楽と同じ題名の歌詞を取得する

            // 選択した音楽と同じ題名の出現ノートを取得する
            

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}